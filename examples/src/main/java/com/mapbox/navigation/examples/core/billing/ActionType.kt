package com.mapbox.navigation.examples.core.billing

enum class ActionType {
    StartSession,
    StopSession,
    RequestRandomRoute,
    Request2LegRoute,
    SetAvailableRoute,
    RemoveRoute,
}
