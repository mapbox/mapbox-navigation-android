- Fixed a race condition in MapboxNavigation between switching to an alternative route and setting a new route. The race could cause a stale route ID to be applied, leaving the route line visible on the map even after the (new) route had ended.